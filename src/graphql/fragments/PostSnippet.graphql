fragment PostSnippet on Post {
    id
    body
    createdAt
    username
    verified
    image
    likeCount
    likes {
      username
    }
    commentCount
    comments {
      username
      createdAt
      body
    }
}